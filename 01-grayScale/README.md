converts a picture into grayscale once using the `cv2,cvtColor` and once using luminescence formula and finds differences.

> **NOTE:** for better output the 'difimg.jpg' pixel values are multyplied by 5
